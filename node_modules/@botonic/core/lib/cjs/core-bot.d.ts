import { Inspector } from './debug';
import { BotRequest, BotResponse, Locales, ResolvedPlugins, Route, Routes, Session } from './models';
import { BotonicOutputParser } from './output-parser';
import { Router } from './routing';
export interface CoreBotConfig {
    appId?: string;
    defaultDelay?: number;
    defaultRoutes?: Route[];
    defaultTyping?: number;
    inspector?: Inspector;
    locales: Locales;
    plugins?: any;
    renderer: any;
    routes: Routes;
    theme?: any;
}
export declare class CoreBot {
    appId?: string;
    defaultDelay?: number;
    defaultRoutes: Route[];
    defaultTyping?: number;
    inspector: Inspector;
    locales: Locales;
    plugins?: ResolvedPlugins;
    renderer: any;
    rootElement: any;
    router: Router | null;
    routes: Routes;
    theme?: any;
    botonicOutputParser: BotonicOutputParser;
    constructor({ renderer, routes, locales, theme, plugins, appId, defaultTyping, defaultDelay, defaultRoutes, inspector, }: CoreBotConfig);
    getString(id: string, session: Session): string;
    setLocale(locale: string, session: Session): void;
    input({ input, session, lastRoutePath, dataProvider, }: BotRequest): Promise<BotResponse>;
}
