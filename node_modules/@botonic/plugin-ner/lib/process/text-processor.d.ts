import { Preprocessor } from '@botonic/nlp/lib/preprocess/preprocessor';
import { Tensor2D } from '@tensorflow/tfjs';
export declare class TextProcessor {
    readonly vocabulary: string[];
    readonly preprocessor: Preprocessor;
    constructor(vocabulary: string[], preprocessor: Preprocessor);
    process(text: string): {
        sequence: string[];
        input: Tensor2D;
    };
    private maskUnknownTokens;
    private encodeSequence;
}
