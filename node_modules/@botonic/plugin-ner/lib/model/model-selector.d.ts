import { ModelSelector } from '@botonic/nlp/lib/model/model-selector';
import { NerConfig } from '@botonic/nlp/lib/tasks/ner';
import { Locale } from '@botonic/nlp/lib/types';
import { LayersModel } from '@tensorflow/tfjs';
import { NamedEntityRecognizer } from './ner';
export declare class NerModelSelector extends ModelSelector<NamedEntityRecognizer, NerConfig> {
    static build(locales: Locale[]): Promise<NerModelSelector>;
    protected createModel(config: NerConfig, model: LayersModel): NamedEntityRecognizer;
}
