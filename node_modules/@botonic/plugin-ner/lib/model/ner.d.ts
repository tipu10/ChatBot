import { NerConfig } from '@botonic/nlp/lib/tasks/ner';
import { Entity } from '@botonic/nlp/lib/tasks/ner/process';
import { LayersModel } from '@tensorflow/tfjs';
export declare class NamedEntityRecognizer {
    private readonly config;
    private readonly model;
    private readonly textProcessor;
    private readonly predictionProcessor;
    constructor(config: NerConfig, model: LayersModel);
    recognize(text: string): Entity[];
}
