import { IndexedItems } from './indexed-items';
declare type OneHotVector = number[];
export declare class OneHotEncoder {
    readonly items: IndexedItems;
    constructor(items: IndexedItems);
    encode(sequence: string[]): OneHotVector[];
    private encodeItem;
    private indexToOneHotVector;
    decode(sequence: OneHotVector[]): string[];
    private decodeOneHotVector;
    private oneHotVectorToIndex;
}
export {};
