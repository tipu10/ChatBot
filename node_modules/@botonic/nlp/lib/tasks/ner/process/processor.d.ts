import { Sample } from '../../../dataset/input-data-parser';
import { LabelEncoder } from '../../../encode/label-encoder';
import { OneHotEncoder } from '../../../encode/one-hot-encoder';
import { Preprocessor } from '../../../preprocess/preprocessor';
import { InputData, OutputData } from './types';
export declare class Processor {
    readonly preprocessor: Preprocessor;
    readonly tokenEncoder: LabelEncoder;
    readonly entityEncoder: OneHotEncoder;
    constructor(preprocessor: Preprocessor, tokenEncoder: LabelEncoder, entityEncoder: OneHotEncoder);
    process(samples: Sample[]): {
        x: InputData;
        y: OutputData;
    };
    generateInput(text: string): {
        sequence: string[];
        input: InputData;
    };
    private processSample;
    private processText;
    private maskUnknownTokens;
    private processTokens;
    private processEntities;
}
