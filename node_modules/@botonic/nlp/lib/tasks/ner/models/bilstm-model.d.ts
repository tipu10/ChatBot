import { LayersModel, Tensor2D } from '@tensorflow/tfjs-node';
import { NerModelParameters } from './types';
export declare function createBiLstmModel(maxLength: number, entities: string[], embeddingsMatrix: Tensor2D, params?: NerModelParameters): LayersModel;
