import { Locale } from '../../types';
import { WordEmbeddingStorage } from '../types';
import { EmbeddingsDimension, EmbeddingsType } from './types';
export declare class DatabaseStorage implements WordEmbeddingStorage {
    readonly locale: Locale;
    readonly type: EmbeddingsType;
    readonly dimension: EmbeddingsDimension;
    private database;
    private statement;
    compatible: boolean;
    private constructor();
    static with(locale: Locale, type: EmbeddingsType, dimension: EmbeddingsDimension): Promise<DatabaseStorage>;
    private checkCompatibility;
    private initialize;
    private checkEmbeddingsFile;
    getWordEmbedding(word: string): Promise<number[]>;
    finish(): void;
}
