import { Locale } from '../types';
import { PreprocessEngines } from './types';
export declare enum SEQUENCE_POSITION {
    PRE = 0,
    POST = 1
}
export declare class Preprocessor {
    readonly locale: Locale;
    readonly maxLength: number;
    engines: PreprocessEngines;
    constructor(locale: Locale, maxLength: number);
    private loadEngines;
    preprocess(text: string, paddingValue: string): string[];
    normalize(text: string): string;
    tokenize(text: string): string[];
    removeStopwords(tokens: string[]): string[];
    stem(tokens: string[]): string[];
    pad(tokens: string[], value: string, position?: SEQUENCE_POSITION): string[];
    truncate(tokens: string[], position?: SEQUENCE_POSITION): string[];
}
