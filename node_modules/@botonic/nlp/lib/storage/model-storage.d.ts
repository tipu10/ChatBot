import { LayersModel } from '@tensorflow/tfjs-node';
export declare class ModelStorage {
    readonly MODEL_FILENAME = "model.json";
    load(path: string): Promise<LayersModel>;
    save(model: LayersModel, path: string): Promise<void>;
}
