import { Locale } from '../types';
export interface NlpTaskConfig {
    locale: Locale;
    maxLength: number;
    vocabulary: string[];
}
export declare class ConfigStorage<T extends NlpTaskConfig> {
    readonly CONFIG_FILENAME = "config.json";
    load(path: string): T;
    save(config: T, path: string): void;
}
