import { LayersModel } from '@tensorflow/tfjs';
import { NlpTaskConfig } from '../storage';
import { Locale } from '../types';
export declare abstract class ModelSelector<M, C extends NlpTaskConfig> {
    readonly locales: Locale[];
    readonly modelsBaseUrl: string;
    private models;
    protected constructor(locales: Locale[], modelsBaseUrl: string);
    protected load(): Promise<this>;
    private loadModelsInfo;
    private loadModels;
    select(locale: Locale): M;
    protected abstract createModel(config: C, model: LayersModel): M;
}
