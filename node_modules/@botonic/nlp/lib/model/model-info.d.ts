import { LayersModel } from '@tensorflow/tfjs';
import { NlpTaskConfig } from '../storage';
import { Locale } from '../types';
export declare class ModelInfo<C extends NlpTaskConfig> {
    readonly locale: Locale;
    readonly uri: string;
    static readonly CONFIG_FILENAME = "config.json";
    static readonly MODEL_FILENAME = "model.json";
    private readonly config;
    private readonly model;
    constructor(locale: Locale, uri: string);
    getConfig(): Promise<C>;
    getModel(): Promise<LayersModel>;
}
