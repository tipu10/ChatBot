import { ClassWeight, LayersModel, Tensor } from '@tensorflow/tfjs-node';
export declare type ModelEvaluation = {
    loss: number;
    accuracy: number;
};
export declare class ModelManager {
    readonly model: LayersModel;
    constructor(model: LayersModel);
    train(x: Tensor, y: Tensor, args: {
        epochs: number;
        batchSize: number;
        classWeight?: ClassWeight;
    }): Promise<void>;
    evaluate(x: Tensor, y: Tensor): Promise<ModelEvaluation>;
    predict(x: Tensor): Tensor;
}
